/** @type {import('tailwindcss').Config} */
export default {
  // All theme customizations are now in index.css.
  // The content paths are automatically detected by the Vite plugin.
  // darkMode: 'class' is the default in Tailwind v4.
};
